.Remove save command - all changes to the password 
records in the file should be immediately saved.

.Separate the keepass console from the libkeepass 
 (different repositories). libkeepass should have 
 unit tests. The console is a demo app not a 
 test harness! 